Approved.

Implement Phase 3B exactly as proposed.

Do not redefine the phase.
Do not expand the scope.
Do not add extra architecture.
Do not add routes, UI, prompts, streaming, retries, rate limiting, or domain logic.

Phase 3B scope is exactly:

Create these new files only:
- server/lib/ai/types.ts
- server/lib/ai/errors.ts
- server/lib/ai/runner.ts

Keep these existing files unchanged unless a minimal import/export adjustment is strictly required:
- server/lib/ai/config.ts
- server/lib/ai/service.ts
- server/lib/ai/usage.ts

Phase 3B requirements:

1) server/lib/ai/types.ts
Create shared runtime types:
- AiCallContext
- AiCallResult

AiCallContext must contain:
- feature: string
- tenantId?: string | null
- userId?: string | null
- model?: AiModelKey

AiCallResult must contain:
- text: string
- usage: {
    input_tokens: number
    output_tokens: number
    total_tokens: number
  } | null
- latencyMs: number
- model: string
- feature: string

2) server/lib/ai/errors.ts
Create a typed AI error hierarchy:
- AiError (base)
- AiUnavailableError
- AiTimeoutError
- AiQuotaError
- AiServiceError

Each error should carry:
- message
- feature
- model
- latencyMs

3) server/lib/ai/runner.ts
Create the single orchestration entrypoint:
- runAiCall(context, { systemPrompt, userInput })

Responsibilities:
- resolve model from context.model ?? AI_MODELS.default
- call generateText() from service.ts
- normalize provider/runtime failures into typed AiError subclasses
- call logAiUsage() on both success and failure
- return AiCallResult on success
- rethrow typed AiError on failure

Implementation constraints:
- no business/domain logic
- no prompts
- no routes
- no UI
- no retries
- no streaming
- no rate limiting
- no changes to existing agents
- no changes to run-executor.service.ts
- no unrelated refactors

Logging behavior:
- logAiUsage() must be called for both success and error paths
- success row must include feature, tenantId, userId, model, usage, latencyMs
- error row must include feature, tenantId, userId, model, latencyMs, errorMessage, status="error"

Error mapping rules:
- missing API key => AiUnavailableError
- abort/timeout => AiTimeoutError
- OpenAI 429 => AiQuotaError
- all other provider/runtime failures => AiServiceError

Output required when finished:
1) exact files created
2) exact files modified
3) concise explanation of how each file matches Phase 3B
4) confirmation that nothing outside Phase 3B scope was added

Commit changes on the current branch only.
Do not open PR.
Do not merge.