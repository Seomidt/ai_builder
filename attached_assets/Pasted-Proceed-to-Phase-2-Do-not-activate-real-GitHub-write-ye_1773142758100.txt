Proceed to Phase 2.

Do not activate real GitHub write yet.
Do not implement Vercel deploys yet.
Do not implement full RAG/vectors yet.

Focus on the next correct foundation layer:

PHASE 2 GOALS
1. Implement the artifact system
2. Implement a minimal artifact dependency graph
3. Implement the first real run execution pipeline
4. Implement commit preview generation
5. Connect architecture versions to runtime execution
6. Add run detail views that expose steps and artifacts clearly

REQUIRED CHANGES

1. Expand ai_runs metadata
Add:
- run_number
- title
- description
- goal
- pipeline_version
- tags
- started_at
- finished_at

2. Expand ai_steps metadata
Add:
- step_key
- title
- description
- tags
- status
- started_at
- finished_at

3. Expand ai_artifacts metadata
Add:
- artifact_type
- title
- description
- tags
- version
- path
- content_json or content_text

4. Add artifact dependency support
Create a simple artifact_dependencies table:
- id
- organization_id
- from_artifact_id
- to_artifact_id
- dependency_type
- created_at

5. Implement the first execution pipeline:
Create Run
→ planner_agent
→ ux_agent
→ architect_agent
→ persist artifacts
→ build commit preview

6. Implement commit preview support
The run detail must show:
- branch name
- commit title
- commit body
- run tags
- list of files/artifacts that would be written later

7. Architecture runtime integration
Architecture versions must now drive runtime behavior:
- enabled agents
- execution order
- expected outputs

8. Agent contracts
Implement typed contracts for:
- planner_agent
- ux_agent
- architect_agent
- review_agent

Each agent must define:
- agent key
- title
- description
- input contract
- output artifact types
- execution handler

9. UI updates
Add Run Detail pages with:
- summary
- status
- steps
- artifacts
- commit preview
- future dependency section

10. Keep provider logic isolated
Do not put GitHub-specific logic in the orchestrator.
Use utility/builders for:
- buildBranchName()
- buildCommitMessage()
- buildRunTags()

11. Do not implement full GitHub write yet
Keep this phase side-effect safe.
We only want artifact generation and commit preview now.

DELIVERABLES
- new migrations
- updated repositories/services
- updated routes
- run execution service
- run detail UI
- artifact UI
- commit preview UI
- short README update describing Phase 2 behavior